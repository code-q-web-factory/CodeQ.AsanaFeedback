#
# The prepare endpoint must be reachable without a Neos backend session
# (anonymous feedback is gated by configuration inside the controller, not
# by the security framework). Without this grant Neos'
# "Neos.Neos:AllControllerActions" privilege would reject every request.
#
privilegeTargets:
  'Neos\Flow\Security\Authorization\Privilege\Method\MethodPrivilege':
    'CodeQ.AsanaFeedback:PrepareFeedback':
      matcher: 'method(CodeQ\AsanaFeedback\Controller\FeedbackController->(prepare|config|frontendConfig)Action())'

roles:
  'Neos.Flow:Everybody':
    privileges:
      -
        privilegeTarget: 'CodeQ.AsanaFeedback:PrepareFeedback'
        permission: GRANT
