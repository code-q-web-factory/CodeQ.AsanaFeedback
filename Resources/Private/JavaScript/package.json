{
  "name": "codeq-asanafeedback-widget",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs && npm run build --prefix ../BackendUi",
    "build:frontend": "node build.mjs"
  },
  "dependencies": {
    "fabric": "^6.7.0",
    "html-to-image": "^1.11.13"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
